2. How do Generics allow you to build reusable components and functions that stay strictly typed regardless of the data structures passed in?

1. একই কোড বিভিন্ন data type এর সাথে কাজ করতে পারে, কিন্তু type ভুল হওয়ার সুযোগ থাকে না।

2. code clean হয়।

3. একই function সব ধরনের data দিয়ে ব্যবহার করা যায়।